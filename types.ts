export interface TimelineStage {
  id: number;
  title: string;
  duration: string;
  items: string[];
}

export interface StoreFormat {
  title: string;
  area: string;
  profit: string;
  payback: string;
  concept: string;
  imageSeed: string;
}

export interface ContactInfo {
  phone: string;
  email: string;
}